name: bingo
description: "Bingo number generator."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ^3.6.0-216.1.beta

dependencies:
  cupertino_icons: ^1.0.8
  backstreets_widgets: ^0.17.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
